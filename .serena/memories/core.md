# ArchGuard Core

ArchGuard is a local-first CLI tool for detecting architectural drift using LLMs and embeddings.
It compares code changes against Architectural Decision Records (ADRs) stored in markdown files with strict YAML frontmatter.

## References
- `mem:tech_stack` - Languages and core tools
- `mem:suggested_commands` - Common CLI commands
- `mem:conventions` - Code style and project patterns
- `mem:task_completion` - Verification steps
